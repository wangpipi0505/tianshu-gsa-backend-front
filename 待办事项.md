# 天枢 GSA 前端待办

> 更新：2026-08-31

## 已完成

- [x] README：`dev` / `dev:mock`，不再写 `cd front`
- [x] `VITE_USE_MOCK` / `npm run dev:mock`；**全部 mock 保留**
- [x] bootstrap：非 mock 时 `fetchDemoToken` + Bearer
- [x] `npm run check-seed-sync` 已绿
- [x] 接新后端后工作台可打开并出图（JWT + snapshot）

## 未完成

- [x] 浏览器已点智能体「敌方重点突防战机」，回显含 `Target-001`
- [x] 图层显隐落库已由后端 `layerVisibilityPersists` 验收；前端「图层控制」抽屉可打开
- [ ] 未明确要求前不要 push
- [ ] 真 LLM：需配置 `GSA_LLM_API_KEY` 后再联调

## 启动

```bash
npm run dev        # 接后端 18086
npm run dev:mock   # 纯前端原型
npm run check-seed-sync
```
